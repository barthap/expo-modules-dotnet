# Documentation Archive

This directory stores historical planning and execution artifacts. These files
are useful for provenance, but they are not authoritative over current code,
tests, or `docs/specs/`.

## Contents

- `agent-plan/`: early architecture, spike, verification, and repo-strategy
  planning docs.
- `spike-results/`: completed proof notes and command evidence.
- `superpowers/specs/`: one-slice Superpowers design specs.
- `superpowers/plans/`: detailed execution plans produced for past work.
- `learning-guide/`: educational walkthroughs written before this repo's code
  existed; superseded by `docs/specs/` and current code.

## Reading Rule

Use archived files to answer why a direction was chosen or to recover evidence
from a proof. Do not use them as current implementation instructions without
checking the living specs and code first.

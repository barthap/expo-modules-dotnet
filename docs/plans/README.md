# Implementation Plans

Generated by the improve skill (2026-07-08; updated 2026-07-09; reconciled
2026-07-09 evening at `56463734`; reconciled 2026-07-20 at `ea07d69d`).
Execute in
the order below unless dependencies say otherwise. Each executor: read the plan
fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | checks.yml — fast CI lane, ubuntu+windows matrix (no native) | P1 | S | — | DONE — green on ubuntu+windows (node + generator-tests + format). |
| 002 | Autolinking CLI command tests | P1 | M | — | DONE — 4 command test files (resolve/stage/build/link), 79 tests pass, typecheck clean. |
| 003 | Bridge edge-case tests (UTF-8, promises, generator diagnostics) | P2 | M | — (001 recommended first) | DONE — 15 UTF-8 string round trips, 3 double-settlement orderings, and all 11 generator diagnostics covered; managed suite and format green. |
| 004 | Bridge string hygiene (copy elimination + consistent UTF-8 validation) | P2 | S | 003 | DONE — direct UTF-8 createString path, all four PropNameID inputs validated, invalid set/get byte tests added; managed suite and format green. |
| 005 | Docs refresh (roadmap events, autolinking README, platform table, authoring guide) | P3 | M | — | DONE — events marked complete, autolinking README, platform table, and `docs/module-authoring-guide.md` (11-item checklist) landed on `advisor/005-docs-refresh`; advisor-reviewed 2026-07-19. |
| 006 | ArrayBuffer and binary codecs — production implementation | P2 | L | 003 | DONE — ABI v22, runtime-owned storage, low-level wrappers, ModulesCore codecs, generated byte[]/span bindings, and teardown coverage landed. |
| 007 | SharedObject/SharedRef — design spike with identity prototype | P3 | M | — (006 recommended first) | DONE — v23 opaque weak handles, deterministic Hermes GC/release-abandon accounting, and internal per-context identity registry proved; 341 managed tests and format pass. |
| 008 | Linux testhost port + native-tests.yml (heavy lane, ubuntu+windows) | P1 | M–L | 001 | DONE |
| 009 | Windows testhost teardown crash (0xC0000005 at process exit) | P1 | M | 008 | DONE |
| 010 | Darwin installer lifecycle locking | P1 | M | — | DONE — managed suite, desktop typecheck, Darwin smokes, and formatting pass. |
| 011 | Publish Hermes prebuilts as downloadable artifacts (keyed by `hermes-ref.txt`) instead of Actions cache | P3 | M | 008 | BACKLOG — nice-to-have; removes per-`hermes-ref`-bump cold builds and warms fresh clones/local dev. Not urgent (cache handles steady state once `main` is seeded). |
| 012 | Typed JS facade base classes (`DotnetModule`, `DotnetEventEmitter`) | P2 | M | 005 | DONE — adapter runtime tests, adapter/mobile/desktop typechecks, 341 managed tests, formatting, and privacy scans pass. |
| 013 | camelCase JS naming defaults + `[JS]` property support (Expo Modules 2.0 alignment) | P2 | M | — | DONE — completed 2026-07-19; implicit lower-camel method/record names, direct accessor properties, diagnostics, and lifetime coverage landed. |
| 014 | Typed `[Event]` partial-property members (Expo Modules 2.0 alignment) | P2 | M | 013 | DONE — generator suite, 449 managed tests, mobile and desktop typechecks, format, reflection/owned-wrapper scans, and documentation/privacy checks pass. |
| 015 | Promise capability migration onto runtime-owned long-lived collection | P2 | M | — | DONE — Promise capability state is runtime-owned, settlement/teardown races are covered, and managed opaque handles are leased. |
| 016 | NativeAOT publish check in CI (linux-x64 compile-time proof; end-to-end loader lane deferred) | P1 | S | — | DONE — `1f8d0414` adds the publish-only AOT job; local macOS and Docker Linux publishes plus actionlint passed. |
| 017 | SharedObject public authoring surface | P2 | L | 015, 016 recommended first | DONE — generated exact-type SharedObject authoring with module-owned JS classes, transactional registry pairing, SharedRef<T>, TS facade, ExampleCounter example, and merged docs. |
| 018 | Windows RNW build/deploy reliability + ReactNativeDir resolver | P1/P2 | L | — (needs Windows machine) | BLOCKED — current executor is on macOS; Windows + VS 2026 + RNW 0.81 prerequisite is unavailable. |
| 019 | Typed `[Event]` members on shared objects | P2 | M | 017 | BLOCKED — self-capturing shared listeners need callback-state disposal that current `Expo.JSI` does not provide. |
| 020 | hermes-console-app Linux port + end-to-end loader lane in CI (hostfxr + nativeaot) | P1 | M | 016 | TODO — unblocked 2026-07-22; Plan 016 is DONE at `1f8d0414`. |

Status values: TODO | IN PROGRESS | DONE | OPEN (follow-up) | BACKLOG (nice-to-have) | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

### Plan 009 — Windows teardown crash (DONE)

Intermittent `0xC0000005` segfault in `Expo.ModulesCore.Tests` at process
teardown on the `windows-latest` runner. Reproduced 2026-07-09 on native-tests
run `29035088584` with a `windows-crash-dump` artifact; dump analysis
confirmed the root cause: the async promise capability was disposed off the
runtime thread in `JavaScriptPromiseScheduler.SettleAsync`. Fix (dispose
inside the scheduled runtime callback) advisor-reviewed, rebase-merged into
`main` 2026-07-09. The bug lived in shared `Expo.JSI` managed core — it
affected all hosts (real RN apps included), not just the testhost. The same
branch also fixed a pre-existing `Counters` struct layout mismatch in the
`Expo.ModulesCore.Tests` fixture (24-byte managed vs 40-byte native, returned
by value — a stack overrun on every `GetCounters` call). Crash was
intermittent: treat as fully closed after several green Windows runs on
`main`; dump instrumentation and the `released_promises_off_runtime_thread`
counter stay armed. Detail in `009-windows-testhost-teardown-crash.md`.

### Reconcile notes (2026-07-20, at `ea07d69d`)

- 007/012/013/014 re-verified DONE on `development`: 449 managed tests pass
  (189 Expo.JSI + 140 Expo.ModulesCore + 120 generator), format check clean,
  example module fully migrated off `SendEventAsync`, legacy `[Events]`
  fixture retained, typed-event and SharedObject scenarios merged into
  `docs/specs/modules-core-boundary.md`, `DotnetModule`/`DotnetEventEmitter`
  exported and extended by the example facade. The 2026-07-19 change
  packages are archived under `docs/archive/changes/`.
- 014 shape deviation accepted: `Action`/`Action<T>` superseded by
  `Func<Task>`/`Func<T, Task>` per the approved delta spec (discarded async
  dispatch tasks would silently lose codec/teardown/scheduling errors;
  blocking risks deadlock on async-only hosts). Recorded in the execution
  note at the top of `014-typed-event-members.md`.
- 011 remains BACKLOG (executor skipped it; still a nice-to-have).

### Execution notes (2026-07-21, at `56893ca2`)

- 015 completed 2026-07-22 via commits `fe79132b`, `d5df8c3e`, `85c39f8d`,
  `1789604b`, `b0eb76d6` (advisor-reviewed).
- 017 completed 2026-07-22 via commits `903fdaa5`, `76da4288`, `e067fac0`,
  `014d0d10`, `2ddcdf9e`, `093bb562`, `ea7a026f`, `326e984a`, `2b4ee365`
  (advisor-reviewed); the Task 1 deviation (construct-trap arguments-array
  contract) is recorded in the archived change plan.
- 016 was attempted without retaining a workflow diff. HostFXR and NativeAOT
  both pass locally on macOS, but the planned Ubuntu job cannot run the
  macOS-only console proof. `scripts/run-hermes-console-app.sh`, the native
  loader, and CMake currently select `osx-*`, `.dylib`, and macOS HostFXR
  assets. A separate Linux-portability plan must land before retrying the
  CI-only lane.
- 016 re-scoped 2026-07-21 at `6db8167c` (advisor review of the blocked
  attempt): the plan now adds a `nativeaot-publish (linux-x64)` compile-time
  job (no Hermes, no runtime), locally provable in Docker. The end-to-end
  loader-matrix lane is gated on two unwritten follow-up plans: console-app
  Linux port (Docker-provable) and Windows NativeAOT port (needs the
  Windows machine). Review corrections: `run-hermes-console-app.ps1`
  already existed at `ea07d69d` (hostfxr-only), and CMake has a full
  Windows HostFXR branch — only Linux is missing.

### Reconcile notes (2026-07-09, at `56463734`)

- 001/002/008/010 re-verified DONE: checks + native-tests green on main,
  test files and workflows in place.
- 003–007 drift-checked and still valid. Bridge drift since `0f6fc760` is
  only the `createClassWithSuperclass` EventEmitter prototype fix
  (`f5e9a334`) — no plan invalidated. For 004: temp copy now at line 770,
  `PropNameID::forUtf8` sites at 1451/1481/1910/2013 (+3 shift).
- The ubuntu native-tests failure on `codex/windows-native-views` is
  branch-specific (NETSDK1100: new `Expo.ModulesCore.Windows.csproj` needs
  `EnableWindowsTargeting` on linux) — not a plan finding.

## Dependency notes

- 015–019 planned 2026-07-20 at `ea07d69d` from the synchronized
  `docs/roadmap.md` (operator selected the four candidates; 019 split out of
  017 on operator decision). Recommended order: 015 → 016 → 017 → 019; 018
  runs independently when a Windows machine is available.
- Per the AGENTS.md "Maturity" rule (added 2026-07-20), plans ship complete
  polished features — no spikes, proofs, design-only milestones, or partial
  surfaces.
- 020 requires 016: both edit `native-tests.yml`, and 020 rewrites the
  gating comment 016 leaves near the matrix. Run 016 → 020. The
  `nativeaot-publish` job stays after 020 lands (fast PR-time signal; the
  `console-app` lane is the runtime proof).
- 017 has no hard dependency but 015 should land first (both touch
  teardown-adjacent test surface; promises stop being the long-lived-state
  exception) and 016 should land first (the NativeAOT lane guards 017's new
  generated code).
- 019 requires 017 (public SharedObject surface). It exists as its own plan
  because per-object events are their own feature (per-instance listener
  machinery), not a missing piece of 017's surface.
- 018 includes the `ReactNativeDir` resolver implementation (operator
  decision 2026-07-20; supersedes the earlier design-only scoping).
- 014 requires 013: it reuses the camelCase naming default and the
  generator's property-scanning machinery introduced there.
- 013/014 come from the Expo Modules 2.0 alignment request (operator-provided
  article, 2026-07-19): the repo's attribute syntax already matches the 2.0
  shape (`[ExpoModule]` name defaulting, no DSL); remaining gaps were naming
  defaults, `[JS]` properties, and typed events. SharedObject stays with plan
  007; generated TypeScript and views were left unplanned (upstream 2.0 also
  ships them later). Stripping the `On` prefix from JS event names was
  rejected to keep the existing `onStatus`-style listener convention.
- 012 should derive its typed events map from 014's `[Event]` declarations
  if 014 lands first; coordinate if executed out of order.

- 004 requires 003 because the UTF-8 boundary corpus and promise tests are the
  regression net for the native `createString` change.
- 001 has no hard dependents, but landing it first means 002's new tests run
  in CI immediately; 008 depends on 001 only for workflow conventions.
- 008 carries the Linux testhost port; until it lands, CI enforces everything
  except the Hermes-backed managed suite (003/004's gate remains local
  `scripts/test-managed.sh`).
- 006 is complete as a production implementation slice covering runtime-owned
  ArrayBuffer lifetime, low-level wrappers, ModulesCore codecs, generated
  bindings, and deterministic teardown coverage. The earlier spike and its
  approved change package are archived provenance; living specs are
  authoritative.
- 007 remains a design spike; its follow-up slice gets planned after GO.
- 007 recommended after 006: SharedRef's flagship use cases (image/file
  handles) pair with binary data, and both widen the ABI — sequence them.
- 010 has no hard dependency, but should land after the active structured
  startup error slice to avoid colliding with in-progress iOS/macOS installer
  edits. Android and Windows are examples only for this plan.

## Direction findings

- **Module authoring guide** — operator re-decided 2026-07-19: advisor-planned
  after all, folded into plan 005 (Step 4), modeled on the upstream
  expo-modules docs structure. Completeness checklist (advisor's high-level guideline only — a
  guide covering all of these is complete):
  1. Project setup: minimal csproj referencing `Expo.ModulesCore`, where the
     module lives in a package (`packages/<name>/dotnet/...` layout).
  2. Autolinking metadata: `expo-module.config.json` shape and how discovery
     finds the module.
  3. Module definition: `[ExpoModule]` class, `[JS]` methods, supported
     parameter/return types and their codecs, enum handling.
  4. Async: `Task`/`Task<T>` methods → promises; threading/scheduling caveats
     module authors must know (what thread callbacks run on).
  5. Events: `[Events]` attribute, `SendEventAsync`, JS-side subscription,
     `OnStartObserving`/`OnStopObserving`.
  6. Lifecycle: `OnCreate`/`OnDestroy` hooks and teardown guarantees.
  7. Callbacks: `JavaScriptCallback<T>` parameters and their lifetime rules.
  8. JS facade: the TypeScript side (`src/`), typing the native module.
  9. Platform matrix: which platforms, HostFXR (dev) vs NativeAOT (prod)
     loader story, NativeAOT constraints authors must respect (no reflection).
  10. Verification: how to run the module's tests locally
      (`scripts/test-managed.sh`), example-module as the reference.
  11. Troubleshooting: common autolinking/staging failures and where the CLI
      puts artifacts.
  Reference implementation to link throughout: `packages/example-module`.
- **ArrayBuffer and binary codecs** → plan 006 is DONE. Its approved change
  spec and plan, plus the earlier spike, are archived under
  `docs/archive/changes/2026-07-10-arraybuffer-codecs/` and
  `docs/archive/agent-plan/006-arraybuffer-codec-spike.md`; living specs are
  authoritative.
- **Promise long-lived-state migration** → follow-up after plan 006. Move
  retained Promise capability state onto the runtime-owned collection without
  changing Promise settlement scheduling. Cover unresolved-promise teardown,
  settlement/teardown races, and idempotent late disposal.
- **SharedObject/SharedRef** → planned as `007-sharedobject-design-spike.md` (2026-07-08).
- **Typed JS facade** → planned as `012-typed-js-facade.md` (2026-07-19,
  operator review of the authoring guide). Related operator direction noted
  the same day: module authoring in separate repos / app-local modules is a
  planned future workflow (guide §1 carries the note); no plan yet.

## Advisor's roadmap assessment (2026-07-08)

Strategic read of `docs/roadmap.md` direction, recorded for future planning
sessions. The operator agreed with this assessment.

- **Sequencing so far is right.** Bottom-up primitive order (values → host
  functions → promises → scheduling → lifecycle → NativeState/HostObject →
  lazy registry), each landing with spec + tests. Keep that discipline.
- **Biggest strategic gap: NativeAOT proof lags surface growth.** The test
  suite exercises only the HostFXR dev loader; NativeAOT compatibility is a
  hard repo constraint but currently a promise, not a proof. Recommend one
  end-to-end NativeAOT proof (example-module, staged via the autolinking CLI,
  running in a real app) BEFORE the next big ABI expansion — retrofit cost
  only grows as ArrayBuffer/SharedObject widen the ABI.
- **Next capability: ArrayBuffer before SharedObject.** ArrayBuffer is mostly
  mechanical (ABI entries + codec, established pattern) and unblocks the
  data-heavy module class (file, camera, crypto). SharedObject carries real
  design risk (registry-backed native identity per the roadmap note) and
  deserves its own design spike after NativeState settles in.
- **CI architecture (operator-decided 2026-07-08)**: two workflows split by
  cost profile. `checks.yml` (plan 001) = fast lane, ubuntu+windows matrix
  (node checks, CLI/TS tests, pure-managed generator tests, format).
  `native-tests.yml` (plan 008) = heavy lane (Hermes cache, testhost, managed
  suite), ubuntu+windows matrix from day one (Windows leg merged into 008
  scope 2026-07-08; ps1 scripts from commit `e380065e` make it cheap). No
  macOS runners for now (dev happens on the operator's Mac; mac runners are
  10× minutes). Future lanes grow as matrix rows in `native-tests.yml`:
  NativeAOT loader lane (vehicle: `apps/hermes-console-app`, gated on the
  E2E proof) → RNW as a separate workflow → Android (incl. Maestro e2e) →
  iOS/macOS last.
  `experiments/*` are hello-world smoke apps — CI ignores them.
- **Windows lane needs a heartbeat.** The repo exists as the
  expo-modules-windows successor, yet desktop-app sits on the RN 0.81 catalog
  while mobile moved to 0.86 (deliberate). Without a periodic Windows E2E
  check, the flagship platform can quietly rot while macOS-hosted Hermes
  tests stay green.
- **After ArrayBuffer: module authoring guide.** API will be stable enough
  that the docs gap becomes the adoption bottleneck (see Direction findings
  above).

## Findings considered and rejected

(So nobody re-audits these.)

- GCHandle leak in `CreateHostFunction`: false — error path releases the
  handle (`Expo.JSI/JavaScriptRuntime.cs`, release on `!result.IsOk`).
- Exception messages propagated to JS: by-design structured error propagation
  per `docs/specs/host-functions-and-errors.md`.
- "Validate raw callback function pointers at the C ABI": not possible;
  rejected as a non-finding.
- `Substring(1)` bounds in generator `LowerCamel`: safe in .NET semantics.
- `int32` overflow in `isValidUtf8` loop: theoretical only; lengths come from
  managed arrays far below `INT32_MAX`.
- Autolinking tsconfig missing strict mode: false — `strict: true` is set.
- CLAUDE.md "effectively empty": false — it is an `@AGENTS.md` include by design.
- Roslyn generator scaling / property-names double loop: no measured cost;
  not worth doing.
- RN 0.81 vs 0.86 dual workspace catalogs: deliberate (RNW requires 0.81);
  at most a doc note when unifying becomes feasible.
- TypeScript version drift (autolinking `~5.9.3` vs catalog `~6.0.3`) and
  transitive advisories in desktop-app build tooling (xmldom HIGH, uuid
  moderate via RNW CLI / expo config-plugins): real but small; excluded from
  the selected plan set 2026-07-08. Revisit if desktop tooling is touched.

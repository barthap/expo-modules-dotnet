plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = 'expo.modules.csharpv2'
version = '0.7.10'

android {
  namespace "expo.modules.csharpv2"
  defaultConfig {
    versionCode 1
    versionName "0.7.10"

    externalNativeBuild {
      cmake {
        arguments "-DANDROID_STL=c++_shared",
          "-DANDROID_SUPPORT_FLEXIBLE_PAGE_SIZES=ON",
          "-DREPO_ROOT=${project.rootDir}/../../.."
        cppFlags "-std=c++20"
      }
    }
  }
  buildFeatures {
    prefab true
  }
  externalNativeBuild {
    cmake {
      path "src/main/cpp/CMakeLists.txt"
    }
  }
  sourceSets {
    main {
      jniLibs.srcDirs = ["src/main/jniLibs"]
    }
  }
  lintOptions {
    abortOnError false
  }
}

dependencies {
  implementation 'com.facebook.react:react-android'
  compileOnly 'com.facebook.fbjni:fbjni:0.5.1'
}
